import { NumbersOnlyDirective } from './numbers-only';

describe('NumbersOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new NumbersOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
